# Copy to .env to override the API URL (production).
# In dev, Vite proxies "/api" to http://localhost:5000 (see vite.config.js),
# so no config is needed locally.
VITE_API_URL=http://localhost:5000/api
